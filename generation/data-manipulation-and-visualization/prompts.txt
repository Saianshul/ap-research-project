Pandas: Using Pandas, generate code that cleans up a messy dataset.
Matplotlib: Using Matplotlib, generate code that creates an interactive graph where additional information is displayed when hovering over specific elements of the graph.
Seaborn: Using Seaborn, create a pair plot and a heatmap with legends and regression lines (if applicable) for a dataset of student exam scores where these scores are differentiated by color based on if they are passing or failing.